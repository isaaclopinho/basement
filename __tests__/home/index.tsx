import { render, screen } from '@testing-library/react';
import StartGame from '../../src/pages';

describe('Home', () => {
  it('renders a heading', () => {
    // render(<StartGame />);
    // const heading = screen.getByRole('heading', {
    //   name: /welcome to next\.js!/i,
    // });
    // expect(heading).toBeInTheDocument();
  });
});
